size(500, 500)

def draw {
  background(128)
  lineplot(
    points(0, 10) { 
      x => sin( x * mouseX * 5 / width ) 
    } map { 
      case (x, y) => (x, height/2 - (y * mouseY * 250 / height).toInt)
    }
  )
}